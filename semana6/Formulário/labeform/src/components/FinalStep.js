import React from "react";

export default function OpenQuestions() { //export default is used to export a single class, function or primitive from a script file.
    return (
        <div>
            <h3>THE FORM IS OVER</h3>
            <p>Thank you very much for participating! We will be in touch!</p>
        </div>
    );
}
